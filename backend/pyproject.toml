[project]
name = "property-listing-backend"
version = "0.1.0"
description = "Backend API service for the property listing platform"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
